/**
 * Root layout - minimal layout for redirect page
 * The actual HTML structure is in app/[locale]/layout.tsx
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
