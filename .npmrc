# This project has a peer-dependency conflict that requires legacy peer-dep
# resolution. Setting it here keeps local installs, CI (`npm ci`), Vercel, and
# Dependabot's lockfile generation consistent — so Dependabot PRs arrive with an
# in-sync package-lock.json instead of failing `npm ci`.
legacy-peer-deps=true
